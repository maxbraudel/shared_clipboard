/// Global application constants
class AppConstants {
  /// The official application name used across all platforms
  static const String appName = 'Shared Clipboard';
  
  /// Private constructor to prevent instantiation
  AppConstants._();
}
